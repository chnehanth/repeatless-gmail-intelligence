The brand's icon — a Lucide stroke glyph from the curated Repeatless set; use anywhere you need an icon, sized 16/20/24.

```jsx
<Icon name="sparkles" size={20} color="var(--ai)" />
<Icon name="inbox" />
```

Names: search, sparkles, send, inbox, refresh, chevronRight, chevronDown, x, check, newspaper, mail, user, logout, pencil, arrowUp, clock, filter. Default size 20, `strokeWidth` 2, `color` currentColor — set `color="var(--accent)"` for active states, `var(--ai)` for AI moments. For a fuller set, load Lucide from CDN.
