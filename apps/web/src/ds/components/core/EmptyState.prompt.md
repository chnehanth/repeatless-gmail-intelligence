Warm, brief empty state — cleared inbox, no results, fresh chat. Copy is action-pointing, never a dead end.

```jsx
<EmptyState icon="inbox" title="Your inbox is clear."
  description="New mail shows up automatically as it syncs." />
```
