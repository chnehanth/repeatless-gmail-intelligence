Toggle switch — the inbox "Unread only" filter and settings booleans.

```jsx
<Switch label="Unread only" defaultChecked />
```

Track turns electric blue when on; thumb slides with the brand's standard easing.
